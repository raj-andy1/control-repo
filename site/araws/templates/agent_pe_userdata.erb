#!/bin/bash

PE_MASTER='ec2-34-223-231-27.us-west-2.compute.amazonaws.com'

PE_CERTNAME=$(curl -s http://169.254.169.254/latest/meta-data/public-hostname)
curl -k https://$PE_MASTER:8140/packages/current/install.bash | bash -s agent:certname=$PE_CERTNAME extension_requests:pp_role=demo-tomcatserver
